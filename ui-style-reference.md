# 创新支撑平台已有系统 UI 风格参考

> 基于已有系统 5 张截图整理，供 Stage 2 原型设计严格参照。

## 1. 整体布局

```
┌──────────────────────────────────────────────────────────┐
│  Header（浅蓝渐变背景）                                     │
│  [logo] 赣政通创新支撑平台        云上（江西）...  陈龙 ↓  │
├────────┬─────────────────────────────────────────────────┤
│        │  面包屑：模块 / 子模块                            │
│ 侧     │                                                  │
│ 边     │  [Tab 页签] 或 [工具栏按钮区]                       │
│ 导     │                                                  │
│ 航     │  [查询条件区：输入框 + 下拉 + 重置 + 查询]          │
│        │                                                  │
│        │  [数据表格]                                        │
│        │                                                  │
│        │  [分页：共 N 条 | 页码 | N条/页 | 前往__页]         │
└────────┴─────────────────────────────────────────────────┘
```

- 三栏布局：左侧导航 + 顶部 Header + 主内容区
- 左侧导航可折叠，层级菜单，当前项蓝色左边框 + 浅蓝底
- Header 浅蓝渐变背景，左侧 logo + 平台名，右侧单位名 + 用户头像

## 2. 配色体系

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色（按钮/链接/高亮） | #1890FF ~ #165DFF | Ant Design 蓝 |
| Header 背景 | 浅蓝渐变 → 白 | 顶部横向渐变 |
| 侧边栏背景 | 白色 | 菜单项白底，选中项浅蓝底 + 左蓝边框 |
| 表格背景 | 白底 + 浅灰边框 #F0F0F0 | 行 hover 浅灰 |
| 分页当前页 | 蓝色背景 | 白字 |
| 状态开关 | 蓝色开启 / 灰色关闭 | Toggle Switch |
| 弹窗遮罩 | rgba(0,0,0,0.5) | 半透明黑 |
| 正文文字 | #333333 | 主文字色 |
| 次要文字 | #666666 ~ #999999 | 辅助说明 |

## 3. 组件风格

### 3.1 按钮

| 类型 | 样式 | 用途 |
|------|------|------|
| 主操作按钮 | 实心蓝底白字 | 查询、确认、新增 |
| 次操作按钮 | 蓝色描边白底蓝字 | 重置、取消、新增流程 |
| 文字链接 | 蓝色文字 | 表格内操作（详情/编辑/删除） |
| 批量操作 | 蓝色描边按钮 | 批量删除、批量同步、导入、导出 |
| 新增按钮 | 带加号图标 + 描边蓝 | "+ 新增流程"、"+ 新增环节" |

### 3.2 表格

- 表头浅灰底，正文白底
- 行 hover 浅灰高亮
- 操作列：文字链接形式（详情、编辑、删除、新增子机构、更多操作）
- 树形表格：缩进 + 展开/折叠箭头
- 状态列：Toggle 开关
- 序号列：纯数字
- 分页：右下角，"共 N 条" + 页码 + "N条/页" + "前往__页"

### 3.3 查询/筛选区

- 输入框：带 placeholder 灰字提示
- 下拉选择：带 placeholder
- 重置按钮：描边样式
- 查询按钮：实心蓝
- 条件横向排列

### 3.4 弹窗（Modal）

- 居中展示，半透明黑遮罩
- 标题栏带关闭 X
- 底部取消 + 确认按钮
- 穿梭框样式：左侧待选择列表 + 中间转移箭头 + 右侧已选择列表
- 拖拽排序提示："拖动行首图标可调整环节顺序，排序自动保存"

### 3.5 面包屑

- 位于 Header 下方主内容区顶部
- 格式："模块名 / 子模块名"

## 4. 页面清单（从截图识别）

| 截图 | 所属子系统 | 页面 | 关键元素 |
|------|-----------|------|----------|
| 1 | 权限管理 | 授权管理-分级管理员列表 | Tab 页签（管理员/超级管理员/分级管理员）+ 查询区 + 表格 + 分页（共22159条） |
| 2 | 权限管理 | 关联用户弹窗 | 穿梭框双栏选择用户 + 搜索 + 下拉筛选 |
| 3 | 组织机构管理 | 机构管理 | 树形表格 + 批量操作（同步/删除/导入/导出）+ 搜索 + 行操作（详情/新增子机构/更多） |
| 4 | 工单中心 | 工单流程配置 | 查询区 + 新增流程 + 表格（含状态开关）+ 行操作（审批环节/编辑/删除）+ 分页 |
| 5 | 工单中心 | 审批环节管理弹窗 | 拖拽排序 + 新增环节 + 表格（环节名/类型/处理人/时限/转办/超时升级）+ 空状态 |

## 5. 原型设计约束

- 技术栈：Tailwind CSS + Font Awesome（CDN），参照 Ant Design 视觉风格
- 主题色：`--primary: #165DFF`（政务蓝）
- 布局：左侧导航 + 顶部 Header + 主内容区
- 表格风格：白底浅灰边框，行 hover 高亮，操作列为文字链接
- 弹窗：居中 + 遮罩，穿梭框双栏选择
- 分页：右下角标准分页
- 查询区：输入框 + 下拉 + 重置 + 查询，横向排列
- 按钮分级：实心蓝（主操作）/ 描边蓝（次操作）/ 文字蓝（行内操作）
